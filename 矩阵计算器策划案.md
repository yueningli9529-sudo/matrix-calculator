# Matrix Calculator 产品策划案

> 文档状态：讨论稿 v0.2
> 初始日期：2026-08-06
> 最近更新：2026-09-14
> 用途：确定产品方向、界面结构、核心数据模型与分阶段实现路线。本文档可以持续修改，不代表最终技术实现。
>
> 决策说明：第 21 章记录 2026-09-14 已确认的长期产品路线与项目治理方式；如果它与早期章节中的版本范围或实施顺序冲突，以第 21 章为当前方向。Qt v0.1 的详细交付标准将在后续专项讨论中校准。

## 1. 项目定位

Matrix Calculator 不只是一个“输入矩阵并直接输出答案”的计算器，而是一个可以保存矩阵、组织矩阵、执行不同类型计算，并查看或亲自完成演算过程的矩阵工作台。

产品需要同时满足两类使用方式：

- 快速计算：选择已有矩阵和操作，直接查看结果。
- 学习与探索：查看每一步矩阵变化，手动执行初等行变换，通过拖动完成换行，并允许程序从当前步骤继续自动计算。

核心体验是：用户创建的矩阵可以长期保存在 Matrix Library 中，各种计算工作区从 Library 选择矩阵副本进行操作，结果由用户决定是否保存、替换或丢弃。
## 2. 核心设计原则

### 2.1 数据与界面分离

- `Matrix<T>` 和 `Rational` 负责数学数据与运算。
- `QLineEdit`、`QTableWidget` 等 Qt 控件只负责输入和显示。
- 存档系统只保存矩阵数据，不能保存 QWidget、Layout 或其他界面对象。
- 计算逻辑不能依赖某个具体页面，多个工作区应能复用同一套核心能力。

### 2.2 原矩阵默认不被破坏

- 工作区从 Matrix Library 取得矩阵副本。
- 计算过程中产生 working matrix 或 result matrix。
- 只有用户明确选择 `Replace Existing` 时，才覆盖原存档。
- 结果默认提供 `Save as New`、`Replace Existing`、`Use Temporarily` 和 `Discard`。

### 2.3 自动操作与手动操作使用同一种步骤模型

步骤可以来自：

- 自动算法。
- 用户通过操作面板执行。
- 用户拖动矩阵行。
- 未来对用户输入的表达式进行解析，例如 `R2 = R2 - 3R1`。

无论来源是什么，最终都应转换成结构化的 Matrix Operation，执行后生成统一的 Step History。

### 2.4 不强迫所有计算使用同一种页面

单矩阵行变换、双矩阵运算和线性映射的交互方式不同。它们共享 Matrix Library 和结果管理规则，但使用不同的工作区。

## 3. 整体产品结构

```text
Matrix Library
    ↓ 选择矩阵副本
Calculation Workspace
    ├── Row Operations
    ├── Matrix Operations
    └── Linear Mapping
    ↓ 产生结果
Save as New / Replace Existing / Use Temporarily / Discard
```

主窗口建议由以下部分组成：

```text
MainWindow
├── Menu Bar
├── Tool Bar
├── Matrix Library（可停靠侧栏）
├── Workspace Area（中心工作区）
│   ├── Matrix Overview
│   ├── Row Operations
│   ├── Matrix Operations
│   └── Linear Mapping
└── Status Bar
```

中心工作区可以使用页面切换结构。用户通过菜单、工具栏或 Library 中的操作入口打开对应工作区，不要求始终显示所有页面。

## 4. 当前开发基础

目前项目已经具备：

- 泛型 `Matrix<T>` 核心类。
- `Rational` 类型及其基本运算。
- `Matrix<Rational>`、`Matrix<int>`、`Matrix<double>` 三种矩阵类型。
- Dialog 中动态生成矩阵输入控件。
- 对三种数据类型进行输入读取。
- MainWindow 通过 action 打开输入 Dialog。
- Dialog 点击 OK 后将编辑结果返回 MainWindow。
- 使用 `QTableWidget` 显示 Current Matrix。

当前输入系统可以作为后续 Matrix Library 的“新建/编辑矩阵”入口。MainWindow 中已经迁移到 Dialog 的旧输入代码和无用头文件可以在功能稳定后清理。

## 5. 矩阵输入 Dialog

### 5.1 功能

- 选择行数、列数和数据类型。
- 根据参数 Build 输入网格。
- 输入 Rational、Int 或 Double 元素。
- Clear 当前输入。
- OK：读取并验证输入，成功后关闭并返回矩阵。
- Cancel：放弃本次修改，不影响 Current Matrix 或 Library。

### 5.2 正确的数据流程

```text
打开 Dialog
→ 选择类型和尺寸
→ Build 输入网格
→ 用户输入
→ OK
→ load_input()
→ 更新 editing_matrix_
→ accept()
→ 调用方取得矩阵
```

读取必须发生在 `accept()` 之前，否则 MainWindow 可能先取得旧矩阵。

### 5.3 后续输入校验

- Rational 分母不能为空时按默认值 1 处理，或者要求用户明确输入。
- 分母不能为 0。
- `toInt()` 和 `toDouble()` 应检查转换结果。
- 输入不合法时 Dialog 保持打开，并指出具体单元格。
- Build 会重建输入网格，应明确提示未保存输入可能丢失。
- 编辑已有矩阵时，Dialog 应预填原有数据，而不是只显示 placeholder。

## 6. Matrix Library

Matrix Library 是整个程序的持久数据中心。它保存用户主动存入的矩阵，并在下次启动程序时恢复。

### 6.1 Library 中的矩阵记录

每条记录建议包含：

| 字段 | 作用 |
|---|---|
| ID | 稳定且唯一的内部标识，不依赖显示名称 |
| Name | 用户可见名称，例如 A、Rotation2D |
| Data Type | Rational、Int 或 Double |
| Rows / Columns | 矩阵尺寸 |
| Values | 矩阵元素 |
| Created At | 创建时间 |
| Modified At | 最后修改时间 |
| Notes / Tags | 后续可选扩展 |

名称可以重复时必须通过 ID 区分；如果不允许重名，应在保存时提示用户选择 Replace 或 Rename。

### 6.2 Library 界面

建议使用左侧可停靠面板：

```text
Matrix Library
────────────────────────
A          Rational 3×3
B          Double   2×2
Basis      Rational 3×3
────────────────────────
[New] [Edit] [Duplicate] [Delete]
```

基本交互：

- 单击：选中矩阵并显示预览。
- 双击：打开编辑 Dialog。
- New：打开空白输入 Dialog。
- Edit：编辑副本，确认后更新对应记录。
- Duplicate：创建新 ID 和新名称，数据相同。
- Delete：需要确认；以后可以加入回收站。
- 右键菜单：发送到 Row Operations、作为 Left Operand、作为 Right Operand、作为 Mapping Input 等。

### 6.3 计算时的使用规则

- 工作区通过 ID 选择 Library 记录。
- 开始 Session 时复制矩阵数据。
- Session 中的修改不自动写回 Library。
- 结果保存时生成新记录，或由用户明确覆盖指定记录。

## 7. 存档系统

### 7.1 第一阶段：自动持久化 Matrix Library

第一版使用 JSON 文件保存 Library：

- 程序启动时加载。
- 新建、编辑、删除或替换记录后自动保存。
- 文件放在 Qt 提供的应用数据目录，而不是源码目录或 build 目录。
- 使用安全写入方式，避免程序中途退出导致文件损坏。
- 文件需要保存 schema version，便于未来升级格式。

Rational 应保存为结构化数据：

```json
{
  "numerator": 2,
  "denominator": 3
}
```

示意格式：

```json
{
  "version": 1,
  "matrices": [
    {
      "id": "stable-id",
      "name": "A",
      "type": "rational",
      "rows": 2,
      "columns": 2,
      "values": [
        [
          { "numerator": 1, "denominator": 2 },
          { "numerator": 0, "denominator": 1 }
        ],
        [
          { "numerator": 3, "denominator": 1 },
          { "numerator": 4, "denominator": 5 }
        ]
      ]
    }
  ]
}
```

### 7.2 第二阶段：Workspace Archive

以后可以支持完整工作区存档：

- 多个矩阵。
- 当前选中的工作区。
- Calculation Sessions。
- Step History。
- 未保存结果。
- 页面布局和 Dock 状态。

Matrix Library 负责日常自动保存；Workspace Archive 负责用户主动导入、导出、备份和分享完整项目。两者不应在第一阶段混为一套复杂系统。

## 8. Matrix Overview / Current Matrix

Matrix Overview 用于查看当前选中的 Library 矩阵：

- 显示名称、类型、尺寸。
- 使用只读表格显示元素。
- 提供 Edit、Duplicate、Delete 和 Send To 等入口。
- 不直接通过表格修改数据；编辑统一进入 Dialog。

Current Matrix 不再是应用中唯一的矩阵，而是 Matrix Library 当前选中的记录或工作区当前使用的矩阵。

## 9. Row Operations 工作区

这是专门在一个矩阵副本上进行操作的学习与演算模式。

### 9.1 工作区组成

```text
Source Matrix / Working Matrix
────────────────────────────────────────
Step History        │ Step Comparison
步骤列表            │ Before → After
────────────────────────────────────────
Manual Operations   │ Auto Operations
```

### 9.2 Step History

示例：

```text
Step 0: Original Matrix
Step 1: Swap R1 and R2
Step 2: R1 ← R1 / 2
Step 3: R2 ← R2 − 3R1
...
Final: RREF
```

默认选中最后一步，因此只想看结果的用户可以立即看到最终答案。点击任意步骤时：

- 左侧或上方显示上一步矩阵。
- 右侧或下方显示当前步骤矩阵。
- 高亮本次改变的行或单元格。
- 显示规范化的操作说明。

Step 0 没有上一步，只显示 Original Matrix。

### 9.3 手动初等行变换

第一版支持：

- Swap：`Ri ↔ Rj`。
- Scale：`Ri ← kRi`。
- Add：`Ri ← Ri + kRj`。

用户通过结构化控件选择操作、行号和系数，点击 Execute 后生成一个新 Step。

未来可以支持文字表达式输入，例如：

```text
R2 = R2 - 3R1
```

文字必须先解析成结构化操作，再执行。不能只把用户输入的字符串当成 Step。

### 9.4 拖动换行

- 只允许拖动行标题或专用拖动手柄。
- 拖动目标行需要高亮。
- Drop 的语义固定为交换两行，而不是插入并移动其他行。
- 完成后自动生成 `Swap Ri and Rj` Step。
- 单个单元格不能被自由拖动，避免破坏矩阵结构。

### 9.5 自动计算与手动计算统一

用户可以：

- 完全手动执行步骤。
- 从原矩阵直接 Auto RREF。
- 手动执行几步后，选择 Auto Complete。
- 查看程序自动生成的所有步骤。

自动算法和手动操作必须生成相同类型的 Matrix Operation 和 Step，不能维护两套独立算法结果。

### 9.6 历史行为

- 点击旧步骤默认只查看，不改变 Working Matrix。
- Undo 撤销最后一个实际步骤。
- Redo 可以在后续版本加入。
- 如果用户希望从旧步骤重新开始，应提供明确的 `Continue From This Step`。
- 第一版可以在继续旧步骤时删除其后的分支；以后再考虑树状分支历史。

## 10. Matrix Operations 工作区

用于两个矩阵之间的运算：

```text
[Matrix A ▼]  [ + / − / × ▼ ]  [Matrix B ▼]  =  Result
```

第一阶段支持：

- 加法。
- 减法。
- 矩阵乘法。

规则：

- 运算前检查尺寸兼容性。
- A 和 B 不被修改。
- Result 初始是临时结果。
- 用户选择 Save as New、Replace Existing 或 Discard。
- 结果可以发送到 Row Operations 或 Linear Mapping。

Matrix Operations 默认直接展示结果。可以提供可折叠的 Calculation Details：

- 加减法按对应元素解释。
- 乘法可以按结果单元格展示点积过程。
- 不要求与 Row Operations 使用完全相同的 Before/After 页面。

## 11. Linear Mapping 工作区

用于表达“变换矩阵把输入映射到输出”：

```text
Transformation A       Input X          Output
┌──────────────┐      ┌────────┐      ┌────────┐
│      A       │  ×   │   X    │  →   │   AX   │
└──────────────┘      └────────┘      └────────┘
```

功能方向：

- 从 Library 选择 Transformation Matrix。
- 选择向量或包含多个向量的矩阵作为 Input。
- 显示 Output。
- 检查维度是否匹配。
- 保存输出为新矩阵。
- 以后可加入二维/三维几何可视化，但不属于第一版范围。

## 12. 统一的 Calculation Session

不同工作区的 UI 不同，但可以共享 Session 概念：

| Session | 输入 | 输出 | 是否天然具有 Step History |
|---|---|---|---|
| Row Operation Session | 一个矩阵 | Working / Final Matrix | 是 |
| Binary Operation Session | 两个矩阵 | Result Matrix | 可选 |
| Mapping Session | 变换矩阵 + 输入 | Output Matrix | 可选 |

Session 保存本次计算上下文，但不代表 Library 中的永久记录。只有用户主动保存结果，才进入 Library。

## 13. Matrix Operation 与 Step 的概念

### 13.1 Matrix Operation

Operation 是真正可以执行的结构化命令，例如：

- SwapRows(rowA, rowB)
- ScaleRow(row, factor)
- AddScaledRow(targetRow, sourceRow, factor)

Operation 负责表达“做什么”。界面文字只是由 Operation 生成的说明。

### 13.2 Step

一个 Step 至少需要表达：

- Step 编号。
- Operation 类型和参数。
- 用户可读说明。
- 操作前矩阵或对前一状态的引用。
- 操作后矩阵。
- 可选高亮信息，例如受影响的行、pivot 位置。

第一版矩阵规模较小时，可以直接保存每一步的矩阵快照，换取简单可靠的 Previous、Next、Undo 和任意步骤查看。以后矩阵很大时，再考虑只保存 Operation 并重放历史。

## 14. 数据类型规则

应用支持：

- Rational：精确分数，适合行变换、RREF、inverse 和 determinant。
- Double：适合近似数值计算，需要考虑浮点误差和显示精度。
- Int：适合整数输入和封闭于整数的运算，但除法可能产生非整数。

需要明确的产品规则：

- `Matrix<int>` 执行 `Ri ← (1/2)Ri` 时不能继续以 int 保存。
- Row Operations 中遇到分数结果时，建议自动提升为 Rational，并明确提示用户。
- 或者第一版限制 Int 的某些行变换，但这会削弱学习体验。
- Double 的“等于零”和 pivot 判断以后需要使用容差，而不是严格比较。

当前建议：行变换模式优先使用 Rational；Int 在需要除法时自动提升为 Rational。该规则仍需最终确认。

## 15. 软件职责划分

建议按职责逐步拆分，而不是把全部逻辑继续堆入 MainWindow：

```text
Core
├── Matrix<T>
├── Rational
└── 数学运算

Model
├── MatrixVariant / MatrixValue
├── MatrixRecord
├── MatrixOperation
├── CalculationStep
└── CalculationSession

Services
├── MatrixLibrary
├── MatrixSerializer
└── Calculation / Trace 生成

UI
├── MainWindow
├── MatrixInputDialog
├── MatrixLibraryPanel
├── MatrixTableView
├── RowOperationsPage
├── MatrixOperationsPage
└── LinearMappingPage
```

MainWindow 负责页面协调，不负责实现所有数学算法、文件格式和输入解析。

## 16. 错误处理与用户反馈

需要避免直接依赖 `assert` 处理用户可触发的错误，因为 Release 构建可能关闭断言，而且用户需要可理解的提示。

常见错误：

- 分母为 0。
- 输入不是合法数字。
- 行列尺寸不兼容。
- 对非方阵求 determinant 或 inverse。
- 矩阵不可逆。
- Double 计算结果接近奇异。
- 从 Library 删除正在被 Session 使用的矩阵。
- 存档文件损坏或版本不兼容。

错误应显示在 Dialog、工作区提示区域或 Status Bar 中，并尽量指出具体位置和解决方式。

## 17. 第一版范围

### 第一版必须具备

- 新建和编辑 Rational、Int、Double 矩阵。
- Matrix Library 中的命名、选择、复制和删除。
- Library 自动保存和启动加载。
- Current Matrix 只读预览。
- Row Operations 的三种基本行变换。
- Step History 和 Before / After 查看。
- 自动 RREF 并生成步骤。
- 双矩阵加、减、乘。
- 结果 Save as New / Replace / Discard。

### 第一版可以暂缓

- 文字形式的行操作解析。
- 树状步骤分支。
- 完整 Workspace Archive。
- 云同步。
- 二维/三维映射动画。
- 超大矩阵优化。
- 插件系统。

## 18. 分阶段实现路线

### 阶段 0：输入系统收尾

- 确认 Dialog 的 OK 顺序为 load → accept → MainWindow 接收。
- 确认 Dialog 关闭后正确释放。
- 清理 MainWindow 中已经迁移的旧输入代码。
- 为输入失败设计明确返回方式。

验收：三种类型均可从 Dialog 输入，并在 MainWindow 正确显示。

### 阶段 1：MatrixRecord 与内存 Library

- 设计 MatrixRecord。
- 将单个 `matrix_` 扩展为多个命名矩阵。
- 在内存中支持 Add、Update、Duplicate、Remove、Find。

验收：不重启程序时可以管理多个矩阵，并在 Current Matrix 区切换。

### 阶段 2：JSON 持久化

- 确定 version 1 文件格式。
- 实现三种类型的序列化和反序列化。
- 启动加载，修改后安全保存。
- 对损坏文件提供错误提示和备份策略。

验收：关闭并重新打开程序后，保存的矩阵仍然存在且数据一致。

### 阶段 3：Matrix Library UI

- 左侧 Library 面板。
- New、Edit、Duplicate、Delete。
- 矩阵预览和选择。
- 新建/编辑 Dialog 与 Library 串联。

验收：用户不接触文件系统即可管理存档矩阵。

### 阶段 4：手动 Row Operations

- Calculation Session 和 Step 数据结构。
- Swap、Scale、Add 操作。
- Step History 列表。
- Before / After 显示。
- Undo Last Step。

验收：用户可以完全手动把一个小矩阵化到 RREF，并查看每一步。

### 阶段 5：自动 RREF Trace

- 自动算法生成与手动相同的 Operation。
- 支持 Auto RREF 和 Auto Complete From Current。
- 最终结果默认选中。

验收：自动和手动产生的步骤可以在同一个历史界面查看。

### 阶段 6：拖动换行

- 行标题拖动。
- 目标行高亮。
- Drop 生成 SwapRows Operation。
- 支持撤销。

验收：拖动两行后矩阵正确交换，并自动增加一步历史。

### 阶段 7：Matrix Operations

- 从 Library 选择 A 和 B。
- 加、减、乘及尺寸校验。
- Result 管理。
- 可选 Calculation Details。

验收：原矩阵不被修改，结果可以保存为新矩阵。

### 阶段 8：Linear Mapping

- Transformation、Input、Output 三栏。
- Library 选择和维度检查。
- 输出保存。

验收：用户可以选择变换矩阵和输入矩阵，并得到可保存的映射结果。

### 阶段 9：Workspace Archive 与体验完善

- 保存完整 Session 和页面状态。
- 导入、导出和备份。
- 快捷键、主题、布局恢复。
- 更完整的 Undo / Redo。

## 19. 需要继续讨论的决策

- [ ] Library 是否允许矩阵重名？
- [ ] 新建矩阵时名称是否必填？
- [ ] Edit 是直接更新原记录，还是默认生成副本？
- [ ] Int 在出现分数时是否自动提升为 Rational？
- [ ] Double 默认显示多少位小数？
- [ ] Calculation Session 是否需要自动保存？
- [ ] 点击旧 Step 后，`Continue From This Step` 是删除后续步骤还是建立分支？
- [ ] Matrix Library 使用 Dock、固定侧栏还是独立页面？
- [ ] Row Operations 的 Before / After 使用左右布局还是上下布局？
- [ ] 自动 RREF 是否允许用户选择 pivot 策略？
- [ ] Mapping 第一版是否只做数值输出，还是同时做二维几何可视化？

## 20. 产品成功标准

第一阶段的成功不是支持最多运算，而是形成一条稳定、容易理解的数据流：

```text
创建矩阵
→ 保存到 Library
→ 选择矩阵进入合适的工作区
→ 执行直接或逐步计算
→ 查看结果和过程
→ 决定保存、替换或丢弃
→ 下次启动仍可继续使用
```

只要这条主流程稳定，后续增加 determinant、inverse、solve、null space、column space 和几何可视化时，都可以自然接入现有架构。

## 21. 长期产品路线与项目治理（2026-09-14 决策）

### 21.1 当前阶段的判断

项目已经不再只是用于练习 C++ 语法的小程序，而是进入“小型软件产品原型逐步工程化”的阶段。

当前版本暂定为 `v0.1-alpha`。现在需要先完成已有 Qt 产品骨架的收口，暂不继续随意扩展新功能。这里的“收口”不是停止开发，而是完成已经确定的主流程、修正错误、建立测试和交付基线。

Qt 桌面版不是未来会被丢弃的临时版本。它将作为：

- 第一个正式客户端。
- C++ 计算能力的第一套完整应用。
- 后续 Web、移动端和服务端开发时的行为参考。
- 核心能力拆分前的可运行基线。

Qt v0.1 的具体功能验收条件、测试清单和发布条件留到后续专项讨论，不在本章展开。

### 21.2 长期产品定位

长期目标是把 Matrix Calculator 发展为一个以 C++ 数学核心为基础的多客户端矩阵计算与学习平台，而不只是单一 Qt 桌面应用。

目标形态：

```text
                         ┌── Qt Desktop Client
                         ├── React Web Client
User / Client ── API ────┤
                         └── Future Mobile Client
                                  │
                                  ↓
                         C++ Matrix Core
                                  │
                                  ↓
                    Repository / PostgreSQL / Files
```

所有客户端共享同一套业务规则。矩阵运算的权威实现保留在 C++ 核心或调用该核心的服务端，不在 TypeScript、Kotlin 等客户端中分别重写一套算法。

需要明确区分“计算核心”和“后端”：

- `Matrix<T>`、`Rational` 和矩阵算法属于计算核心，它们本身不是后端。
- 后端是持续运行并接收网络请求的程序，它负责调用计算核心、协调数据访问并返回结果。
- 前端负责交互和展示，不重复实现权威矩阵算法。

当前与未来的数据调用关系为：

```text
当前 Qt：Qt UI → 本地调用 C++ Matrix Core

未来客户端：React Web / React Native / KMP
                    ↓ 网络请求
                 Backend
                    ↓ 本地函数调用
              C++ Matrix Core
                    ↓
                PostgreSQL
```

Qt 桌面端不会因为增加后端而被强制改成联网程序。它可以继续在进程内直接调用同一个 C++ Core；只有云端存储、账号同步等网络能力需要经过后端。

### 21.3 总体演进顺序

长期路线分为七个工程阶段。这里的阶段描述产品和架构演进；原第 18 章描述的是早期 Qt 桌面版内部功能实施顺序，两者不是同一层级。

```text
阶段 0：完成并稳定 Qt 桌面版 v0.1
        ↓
阶段 1：提取独立 C++ Matrix Core
        ↓
阶段 2：建立最小 REST 后端
        ↓
阶段 3：建立 React Web 客户端
        ↓
阶段 4：加入 PostgreSQL、用户与历史记录
        ↓
阶段 5：加入截图识别矩阵
        ↓
阶段 6：移动端、部署与生产级完善
```

#### 阶段 0：Qt 桌面版 v0.1

目标是形成第一个稳定、可演示、可测试和可发布的桌面产品版本。阶段内只收口已经确定的功能，不加入截图识别、混合类型运算、复杂可视化等新分支。

#### 阶段 1：独立 C++ Matrix Core

把 `Matrix<T>`、`Rational`、矩阵运算、行操作及必要的领域规则从 Qt 页面和控件中分离，形成不依赖具体 UI 的可测试核心库。

这一步不是重写算法，而是把已有正确能力整理成能够被桌面端和服务端共同调用的权威实现。

#### 阶段 2：最小 REST 后端

建立一个调用 C++ Core 的服务端，先完成一条最小纵向链路，例如：客户端提交两个矩阵和加法请求，服务端校验、计算并返回结果。

第一轮只使用内存数据，不立即加入数据库、账号系统或微服务。通信先采用 REST；它更适合作为浏览器客户端的第一套接口，也便于直接检查和调试。RPC 或 gRPC 只在后续出现明确需求时再评估。

后端首先采用模块化单体结构，不提前拆分微服务。

#### 阶段 3：React Web 客户端

第一个新增客户端优先选择 React Web + TypeScript。它负责完成浏览器中的矩阵输入、请求服务端计算、结果展示和错误反馈，从而建立第一条完整的全栈链路。

Web 客户端可以进行即时输入检查以改善体验，但服务端仍必须重新校验请求，服务端规则才是最终权威。

#### 阶段 4：PostgreSQL、用户与历史记录

在前后端最小闭环已经稳定之后，再加入：

- PostgreSQL 持久化。
- 数据库迁移。
- 用户身份与权限。
- 云端矩阵记录。
- 计算历史或 Session。
- 跨设备访问所需的数据接口。

API 传输对象、C++ 领域对象和数据库表模型应保持职责分离，不能直接把现有 JSON 存档格式同时当作 API 协议和数据库结构。

#### 阶段 5：截图识别矩阵

截图识别属于高价值、高风险的产品能力，暂不进入 Qt v0.1。

正确流程应为：

```text
上传或粘贴截图
→ 识别候选矩阵
→ 显示可编辑确认界面
→ 用户修正并确认
→ 使用现有输入校验
→ 加入 Repository 或进入计算流程
```

OCR 结果不能未经确认直接写入 Repository 或用于正式计算。第一版识别功能可以先让用户手动框选矩阵区域并指定行列数，自动定位矩阵边界属于后续增强。

#### 阶段 6：移动端、部署与生产级完善

移动端在 Web 与服务端边界稳定后再进入正式选型：

- 如果希望延续 React / TypeScript 技术栈，可评估 React Native。
- 如果明确希望学习 Kotlin 并建设原生移动体验，可评估 Kotlin Multiplatform。

由于现有权威计算逻辑是 C++，Kotlin Multiplatform 的“共享 Kotlin 业务逻辑”优势目前不能直接替代 C++ Core，因此它不是当前阶段的默认选择。

这一阶段还包括部署、配置管理、日志、监控、安全、备份、性能和正式发布流程。

### 21.4 现有代码在未来架构中的位置

已有工作不会被整体推翻，其长期归属暂定如下：

| 当前部分 | 未来职责 |
|---|---|
| `Matrix<T>`、`Rational` | 独立 C++ Matrix Core |
| Row Operation、Matrix Step | 计算领域模型或 Calculation 模块 |
| `MatrixRepository` | Repository 接口及本地实现的基础 |
| JSON Serializer | 本地导入、导出和离线存档格式 |
| Matrix Save System | Qt 桌面端的本地存档能力 |
| Qt Pages、Widgets、Models | Desktop Client |
| React Web | REST API Client |
| PostgreSQL | 服务端持久化实现 |

本地 JSON 存档仍然有价值，但它不会自动成为未来的网络 API 或数据库 schema。

### 21.5 演进原则

整个路线遵循“每个阶段只新增一个主要系统边界”的原则：

```text
Qt UI 与计算逻辑 → 独立核心边界
独立核心 → HTTP 服务边界
HTTP 服务 → Web 客户端边界
内存数据 → 数据库边界
文本输入 → 图像识别边界
单客户端 → 多客户端与生产部署边界
```

这样可以减少同时变化的未知因素，并使错误能够定位到具体层次。

不在当前工作目录中立刻大规模移动文件。只有在 Qt v0.1 建立安全版本基线后，才开始按已确认的边界提取核心。

### 21.6 项目管理方式

从当前阶段开始，项目按真实软件项目的节奏推进。教学速度可以放慢，但工程标准不降低。

项目同时维护两条管理线：

1. 产品与技术路线：决定未来做什么，以及能力以什么顺序加入。
2. 交付与质量流程：决定每个里程碑怎样立项、实现、测试、审查和发布。

当前确认的基本规则：

1. 每个阶段都要定义目标、范围、进入条件和退出条件。
2. 一个阶段没有达到退出条件，不进入下一阶段。
3. 当前里程碑的范围一旦冻结，新想法先进入 Backlog，不立即打断开发。
4. 每项功能在编码前先明确需求、验收标准、边界情况和测试思路。
5. “完成”不能只表示能够编译或手动运行一次；它应包含实现、适当测试、代码审查和必要文档。
6. 重要架构选择需要记录原因、备选方案和代价，避免以后只记得结论。
7. 发布版本需要具有可重复构建、明确的已知限制和可定位的版本标记。
8. 页面和客户端不作为核心业务数据的唯一所有者；业务数据与 UI 状态保持职责分离。
9. 客户端校验服务于用户体验，核心或服务端校验负责保证数据正确性。
10. 不为尚未出现的规模问题提前引入微服务、大型框架或多套重复实现。
11. 当前策划案先记录方向、边界和阶段顺序；API 字段、框架配置、跨语言绑定、数据库表等实现细节，在项目真正进入相应阶段时再展开、验证并形成专项设计。

后续将逐步建立并讲解以下项目产物，而不是一次性全部创建：

- Product Backlog。
- 当前 Milestone / 版本范围。
- 功能验收标准。
- 测试计划与自动化测试。
- 架构决策记录（ADR）。
- 缺陷与风险清单。
- 发布检查表、版本标签和 Release Notes。

### 21.7 当前边界与下一步讨论顺序

当前只确认长期方向，不立即修改为全栈目录，也不开始 React、后端、数据库或 OCR 开发。

已讨论但暂不展开的技术主题包括：独立 Core 的具体目录和接口、React Native 与 KMP 的最终选择、REST 与 RPC 的详细协议、数据库 schema、部署方式以及移动端是否需要离线嵌入 C++。这些内容保留为对应阶段的设计任务，不视为已经完成的技术决策。

后续按照以下分类逐项讨论：

1. 产品定位与版本路线。
2. 现有 C++ 代码如何复用，以及独立 Core 应当是什么。
3. 前端选择：Qt、React Web、React Native 与 Kotlin Multiplatform 的关系。
4. 后端边界与 REST API 设计。
5. 数据库、Repository 与数据迁移。
6. 测试、代码审查、持续集成和质量门禁。
7. 部署、日志、监控、安全和版本发布。
8. OCR 与其他高级产品能力。

每次只推进一个主题中的一个关键决策；确认后再进入下一项。

## 22. Qt v0.1 收口盘点（2026-09-14）

> 状态：只读审计草案。此处记录当前事实、发布阻断项和推荐处理顺序；详细验收标准将在处理各板块时逐项确定。

### 22.1 已验证的项目基础

- Git 工作区干净，当前位于 `main`，并与 `origin/main` 同步。
- 已存在 v0.1-alpha 基线提交和较完整的 README。
- 从全新临时构建目录完成 Debug 配置与编译，应用目标构建成功。
- `core/tests.cpp` 中现有 12 项核心测试能够通过，但尚未接入 CMake/CTest，因此还不是可重复执行的正式测试流程。
- JSON 已具有 schema version，并对 UUID、名称、数据类型、尺寸、元素数量、整数范围和有限浮点值进行了基础校验。

### 22.2 当前功能状态

| 板块 | 当前状态 | v0.1 判断 |
|---|---|---|
| `Matrix<T>` 与 `Rational` | 核心功能已存在，已有部分开发测试 | 基本完成，等待正式测试化 |
| 三类型矩阵输入 | Build、编辑和显示主流程已实现 | 存在输入状态与最终校验缺陷 |
| Matrix Widget / Model / Delegate | 编辑与只读显示、分数绘制、尺寸适配已实现 | 基本完成 |
| Repository | Add、Find、只读遍历、UUID 身份已实现 | 满足当前最小流程；删除、修改可后置 |
| Repository 列表与搜索 | 命名、选择、大小写无关搜索已实现 | 主流程完成，刷新状态需修正 |
| 双矩阵运算 | 同类型加、减、乘及尺寸校验已实现 | 主流程完成，过期结果需处理 |
| 手动行操作 | Swap、Add Scaled Row、Scale 与步骤快照已实现 | 主流程完成，会话状态需修正 |
| Before / After 步骤查看 | 正常来源不变时可以工作 | 来源变化时不可靠 |
| JSON 保存与加载 | 正常路径可以往返 | 文件安全和错误反馈未完成 |
| 自动 REF/RREF、Auto Complete | UI 中有入口但没有实现 | 不属于当前 v0.1，应隐藏或禁用 |
| Calculation Details | 仅有空白占位 | 不属于当前 v0.1，应隐藏或禁用 |
| 自动化测试与发布 | 尚未形成 CTest、CI、正式包和版本标签 | 未完成 |

### 22.3 发布阻断项

#### P0-1：InputPage 缺少明确状态

当前 Save 按钮始终尝试从编辑网格读取数据。用户从 Repository 选择一个只读矩阵后再点击 Save，可能出现：

- 从未 Build 时访问空的 `matrixLayout_` 并崩溃。
- 编辑网格尺寸与展示矩阵不一致时访问空单元格并崩溃。
- 尺寸相同时被隐藏的旧编辑网格覆盖当前展示内容，并保存错误矩阵。

首次启动时 UI 虽显示默认 2×2 参数，但尚未真正创建编辑矩阵；直接 Save 还可能把默认的 0×0 Matrix 加入 Repository，而该记录随后会被反序列化规则拒绝，导致存档无法重新加载。

v0.1 需要明确 `Empty / Editing / Displaying` 等页面状态，只有存在有效编辑矩阵时才能执行 Save。

#### P0-2：计算会话没有随来源变化失效

单矩阵步骤非空时，新操作始终从 `steps_.back().resultMatrix_` 继续。切换上方来源矩阵、刷新 Repository 或加载新存档时，旧步骤仍然保留，可能把新选择和旧计算历史混在一起，并在某些行数变化下造成越界风险。

v0.1 需要建立统一规则：源矩阵、Repository 或相关模式变化时，清除或明确失效所有依赖旧输入的步骤、Before/After 和结果。

#### P0-3：当前输入与展示结果可能不一致

双矩阵页面改变 A、B 或运算符后，旧 Result 仍然显示；单矩阵页面改变来源后也可能保留旧 After。界面因此可能把旧结果表现成当前输入的答案。

v0.1 需要让所有派生结果在其输入发生变化时清空、隐藏或标记为过期。

### 22.4 高优先级质量项

1. Save 前需要最终检查每一个输入控件，而不能只依赖 validator；数字转换失败、分母为 0 和类型不兼容必须产生明确结果。
2. 手动行操作的非法系数和不兼容系数目前会静默失败，需要用户可理解的反馈。
3. 保存和加载失败目前被 UI 静默忽略，需要区分并提示失败。
4. 文件保存当前直接覆盖旧 JSON，需要改为安全的原子写入，并确定失败恢复策略。
5. Repository 接受的数据与 JSON 加载规则不完全一致，需要建立“能够加入 Repository 的记录一定能够保存并重新加载”的不变量。
6. 损坏 JSON 中的 `INT_MIN` 与负分母组合可能触发 `Rational` 整数溢出，需要在构造前拦截。
7. Repository 刷新后应重新应用当前搜索关键字。
8. 加载新存档后，需要同时刷新 InputPage、CalculatePage 及所有依赖 Repository 的状态。
9. 大矩阵的尺寸上限、滚动方式和显示能力需要保持一致，避免 UI 允许创建但无法操作的尺寸。
10. 未实现的 Auto Complete、REF/RREF 和 Calculation Details 不应以可操作功能的形式出现在 v0.1。

### 22.5 测试与发布缺口

- 在 CMake 中显式声明项目使用的 C++ 标准和项目版本。
- 把核心测试建立为独立测试目标并接入 CTest。
- 补充 Rational、Matrix、Repository、JSON 往返、损坏存档和 I/O 失败测试。
- 为输入状态、计算来源切换和派生结果失效建立可重复验证。
- 完成干净 Debug/Release 构建与安装测试。
- 确定 License、应用名称、Bundle 标识符和真实版本号。
- 准备 Release Notes、版本标签和第一份可安装发布产物。
- CI、跨平台产物、签名和公证是否进入 v0.1，后续单独确定。

### 22.6 当前确认的 v0.1 边界

- 存档由用户主动选择保存或加载，不强制启动加载或退出保存。
- 双矩阵运算只允许相同数据类型，支持加、减、乘。
- 单矩阵模式支持三种手动初等行操作及步骤回看。
- 自动 REF/RREF、步骤删除与重算、Undo/Redo、混合类型运算、特殊计算过程绘制、OCR、Web、后端和数据库均暂缓。
- Repository 的重命名、覆盖和删除是否必须进入 v0.1，需要在正式范围冻结时确认；当前最小主流程不依赖它们。

### 22.7 推荐处理顺序

```text
1. 修复 InputPage 状态与空矩阵保存问题
2. 建立计算会话重置和派生结果失效规则
3. 补齐输入与用户错误反馈
4. 加固 Repository、JSON 和文件写入
5. 隐藏或禁用 v0.1 不实现的控件
6. 建立 CMake/CTest 自动化测试
7. 完成发布元数据、构建检查和发布清单
8. 复核所有验收条件后标记 v0.1
```

每一项开始前先讨论行为和验收条件，再由实现、审查和测试完成闭环。

## 23. Qt v0.1 范围与交付准则（已确认）

> 决策日期：2026-09-14
> 决策状态：已确认。后续开发、测试和发布以本章为准；如需改变范围，应先记录变更原因并重新确认。

### 23.1 功能范围

Qt v0.1 只承诺以下六项产品能力：

| 功能 | v0.1 提供的能力 |
|---|---|
| 创建矩阵 | 输入和显示 `int`、`double`、`Rational` 三种矩阵 |
| 矩阵仓库 | 命名加入内存 Repository、按名称搜索、选择已有矩阵 |
| 双矩阵计算 | 相同数据类型的矩阵加法、减法和乘法 |
| 单矩阵操作 | Swap、Scale、Add Scaled Row 三种手动初等行操作 |
| 步骤查看 | 保存操作步骤并查看每一步的 Before / After |
| 本地存档 | 用户主动保存或加载整个 Repository，也可以只临时使用 |

自动 REF/RREF、步骤删除与重算、Undo/Redo、计算结果另存、混合类型运算、特殊计算过程绘制、OCR、Web、后端和数据库不属于 v0.1。

Repository 记录修改、重命名、覆盖和删除也暂不列入 v0.1 的强制范围。

### 23.2 InputPage 状态与 Build

InputPage 至少需要区分没有工作矩阵、正在编辑和只读展示三类状态。

- 软件刚打开且用户尚未 Build 时，没有当前工作矩阵，Save 不可用。
- 用户选择合法的行数、列数和数据类型并点击 Build 后，创建对应类型与尺寸的输入网格。
- 新矩阵的所有元素使用相应数据类型的零作为默认值。
- Build 只创建当前工作矩阵，不向 Repository 添加记录。
- Build 成功后进入编辑状态，Save 才可用。
- 已存在未保存输入时再次 Build，需要先询问是否放弃。
- 用户取消重建时，原矩阵、输入值和页面状态保持不变。
- 用户确认后，才用新的类型和尺寸替换原编辑内容。

切换到其他主页面时保留 InputPage 的编辑内容。页面切换本身不视为放弃输入，因此不弹出确认。

### 23.3 输入检查与保存

点击 Save 后必须先验证全部单元格。只有全部合法时才能进入命名与 Repository 保存步骤。

- 空白整数、空白 Double 和空白分子按 `0` 处理。
- 空白分母按 `1` 处理。
- 用户已经输入但格式不合法的内容不能静默转换为零。
- Rational 分母不能为零。
- 错误提示应包含单元格行列位置和原因。
- 验证失败时保留全部输入，Repository 不发生变化。
- 取消命名时保留编辑内容，Repository 不发生变化。
- 名称去除首尾空格后不能为空。
- 名称比较忽略英文大小写；`A` 与 `a` 视为重名。
- v0.1 不允许重复名称，遇到重名时提示用户重新命名。
- 名称合法后生成新的 UUID，再把完整 MatrixRecord 加入 Repository。
- 添加成功后列表立即刷新，页面切换为只读展示，Save 变为不可用。
- Repository 拒绝保存无效 MatrixRecord，保证能够加入的数据也能够序列化并重新加载。

### 23.4 Repository 查看与搜索

- 当前没有未保存输入时，选择 Repository 记录会直接以只读方式显示该矩阵。
- 当前存在未保存输入时，选择其他记录前先询问是否放弃。
- 取消切换时保留编辑状态和输入；确认后才进入选中记录的只读展示。
- 查看已有矩阵不会修改 Repository 数据，也不会把隐藏的编辑网格写回所选记录。
- 搜索根据矩阵名称执行包含匹配，忽略英文大小写，并去除搜索词首尾空格。
- 搜索框为空时显示全部记录；没有匹配项时列表为空，但不删除记录。
- 新增记录或加载存档后，列表刷新并继续应用当前搜索关键词。
- 搜索只改变列表的可见项，不改变当前编辑或展示的矩阵。

### 23.5 单矩阵会话

一组单矩阵步骤属于开始计算时选择的源矩阵。

- 更换源矩阵前，如果已有步骤，需要提示切换会清空当前步骤。
- 取消时继续使用原矩阵并保留全部步骤和显示内容。
- 确认后切换到新矩阵，清空步骤列表、After 和其他依赖旧来源的结果。
- 新的 Apply Step 从新源矩阵开始。
- 离开计算页再返回时，如果源矩阵没有变化，应保留原步骤和当前查看位置。
- 单矩阵与双矩阵模式分别保存自己的界面状态；切换模式不清除另一模式的工作。

查看历史步骤只改变 Before / After 展示，不改变计算链末尾。用户查看旧步骤后点击 Apply Step：

- 新操作仍然基于最后一步结果。
- 成功后追加为新的最后一步并自动选中。
- 既有步骤保持不变。
- 操作区域需要说明新操作会接在最后一步之后。
- 从中间步骤建立分支或删除后续步骤留给后续版本。

### 23.6 行操作参数

行操作参数失败时，不增加步骤、不修改矩阵、不改变既有历史，并保留用户填写内容以便修正。

| 矩阵类型 | v0.1 接受的系数 |
|---|---|
| Int | 整数，例如 `2`、`-3` |
| Rational | 整数或分数，例如 `2`、`3/4` |
| Double | 整数、小数或分数，例如 `2`、`0.5`、`3/4` |

系数格式错误、分母为零、Scale 系数为零，或系数不适用于当前矩阵类型时，需要提示具体原因。输入框的提示文字应随当前矩阵类型变化。

### 23.7 双矩阵计算

- v0.1 只允许两个矩阵的数据类型相同。
- 加法和减法要求行列数都相同。
- 乘法要求 Matrix A 的列数等于 Matrix B 的行数。
- 运算成功后显示结果，不修改 A 或 B。
- 更换 A、B 或运算类型时，立即清空或隐藏旧 Result。
- 新输入必须再次点击 Calculate 才生成新结果。
- 类型或尺寸校验失败时说明具体原因，Result 保持为空，输入矩阵保持不变。

### 23.8 存档与退出

v0.1 的存档由用户主动控制。程序不在启动时强制加载，也不在每次 Repository 修改后自动写文件。

保存存档时：

- 保存当前 Repository 的全部记录，包括空 Repository。
- 尚未加入 Repository 的编辑内容、计算步骤和临时结果不包含在存档中。
- 界面需要让用户理解存档的实际范围。
- 保存成功后给出确认，并把 Repository 标记为已保存。
- 保存失败时说明原因，保留当前内存数据，旧存档保持完整。
- 文件写入使用安全的原子替换方式，避免写入中断损坏旧存档。

加载存档时采用“全部成功才替换”：

- 先在临时 Repository 中完整读取和验证文件。
- 检查 JSON、schema version、UUID、名称、重复名称、数据类型、尺寸、元素数量和元素取值。
- 任意记录无效时拒绝整个文件，当前 Repository 和页面状态保持不变。
- 错误提示尽量包含记录位置、名称和失败原因。
- 完整验证成功后，如果当前有未保存数据，先询问用户是否继续。
- 用户确认后才整体替换 Repository，并清除所有依赖旧 Repository 的编辑、步骤和结果状态。
- InputPage 和 CalculatePage 同步刷新；InputPage 继续应用原搜索关键词。

退出时区分两类内容：

- Repository 自上次成功保存后发生变化，属于未保存的仓库修改。
- 尚未加入 Repository 的输入，以及计算步骤和临时结果，属于临时工作。

Repository 有未保存修改时，退出界面提供“保存存档并退出”“放弃并退出”“取消退出”。只有临时工作时，说明这些内容会丢失，并提供“放弃并退出”和“取消退出”。没有任何变化时可以直接退出。

### 23.9 v0.1 界面范围

只向用户展示已经实现且属于 v0.1 的入口。

单矩阵模式显示源矩阵、Before / After、三种手动行操作、Apply Step 和步骤列表。REF、RREF 和 Auto Complete 暂时隐藏。

双矩阵模式显示 Matrix A、Matrix B、Add / Subtract / Multiply、Calculate 和 Result。空白的 Calculation Details 暂时隐藏。

### 23.10 测试策略

自动测试重点覆盖错误计算、数据损坏和状态混乱三类风险：

1. 数学核心测试：`Rational`、矩阵加减乘和三种行操作。
2. 数据与存档测试：Repository 不变量、JSON 往返、损坏存档、重复名称和非法矩阵。
3. 关键页面状态测试：未 Build 不能 Save、切换源矩阵处理旧步骤、改变双矩阵输入使旧结果失效。

矩阵居中、括号绘制、页面比例和视觉体验采用人工检查。所有自动测试需要建立正式目标并统一通过 CMake/CTest 执行。

### 23.11 正式发布门槛

满足以下条件后，项目才能从 `v0.1-alpha` 标记为正式 `v0.1.0`：

- 六项范围内的主流程全部满足验收规则。
- 没有已知的崩溃、错误计算或数据损坏问题。
- 所有自动测试通过。
- 至少一套持续集成流程能够自动构建并运行测试。
- Debug 和 Release 均能从干净目录构建。
- 未实现的入口已经隐藏。
- macOS 应用能够安装或解压后正常运行。
- README 的构建方法、功能范围和已知限制与实际一致。
- 项目具有正式版本号、License 和 Release Notes。
- 建立 Git 标签 `v0.1.0`。

不影响核心流程的问题可以保留，但必须记录为已知限制。

### 23.12 执行方式

从现在开始按以下闭环逐项推进：

```text
选取一条已确认的验收规则
→ 讨论需要的状态和职责
→ 用户实现或明确授权代为实现
→ 代码审查
→ 添加适当测试
→ 构建并验证
→ 标记该项完成
```

第一项开发任务为 InputPage 状态管理与空矩阵保存问题。开始编码前，先讲清状态表示方式和状态转换。
