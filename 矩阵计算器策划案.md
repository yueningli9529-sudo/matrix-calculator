# Matrix Calculator 产品策划案

> 文档状态：讨论稿 v0.1
> 日期：2026-08-06
> 用途：确定产品方向、界面结构、核心数据模型与分阶段实现路线。本文档可以持续修改，不代表最终技术实现。

## 1. 项目定位

Matrix Calculator 不只是一个“输入矩阵并直接输出答案”的计算器，而是一个可以保存矩阵、组织矩阵、执行不同类型计算，并查看或亲自完成演算过程的矩阵工作台。

产品需要同时满足两类使用方式：

- 快速计算：选择已有矩阵和操作，直接查看结果。
- 学习与探索：查看每一步矩阵变化，手动执行初等行变换，通过拖动完成换行，并允许程序从当前步骤继续自动计算。

核心体验是：用户创建的矩阵可以长期保存在 Matrix Library 中，各种计算工作区从 Library 选择矩阵副本进行操作，结果由用户决定是否保存、替换或丢弃。
## 2. 核心设计原则

### 2.1 数据与界面分离

- `Matrix<T>` 和 `Rational` 负责数学数据与运算。
- `QLineEdit`、`QTableWidget` 等 Qt 控件只负责输入和显示。
- 存档系统只保存矩阵数据，不能保存 QWidget、Layout 或其他界面对象。
- 计算逻辑不能依赖某个具体页面，多个工作区应能复用同一套核心能力。

### 2.2 原矩阵默认不被破坏

- 工作区从 Matrix Library 取得矩阵副本。
- 计算过程中产生 working matrix 或 result matrix。
- 只有用户明确选择 `Replace Existing` 时，才覆盖原存档。
- 结果默认提供 `Save as New`、`Replace Existing`、`Use Temporarily` 和 `Discard`。

### 2.3 自动操作与手动操作使用同一种步骤模型

步骤可以来自：

- 自动算法。
- 用户通过操作面板执行。
- 用户拖动矩阵行。
- 未来对用户输入的表达式进行解析，例如 `R2 = R2 - 3R1`。

无论来源是什么，最终都应转换成结构化的 Matrix Operation，执行后生成统一的 Step History。

### 2.4 不强迫所有计算使用同一种页面

单矩阵行变换、双矩阵运算和线性映射的交互方式不同。它们共享 Matrix Library 和结果管理规则，但使用不同的工作区。

## 3. 整体产品结构

```text
Matrix Library
    ↓ 选择矩阵副本
Calculation Workspace
    ├── Row Operations
    ├── Matrix Operations
    └── Linear Mapping
    ↓ 产生结果
Save as New / Replace Existing / Use Temporarily / Discard
```

主窗口建议由以下部分组成：

```text
MainWindow
├── Menu Bar
├── Tool Bar
├── Matrix Library（可停靠侧栏）
├── Workspace Area（中心工作区）
│   ├── Matrix Overview
│   ├── Row Operations
│   ├── Matrix Operations
│   └── Linear Mapping
└── Status Bar
```

中心工作区可以使用页面切换结构。用户通过菜单、工具栏或 Library 中的操作入口打开对应工作区，不要求始终显示所有页面。

## 4. 当前开发基础

目前项目已经具备：

- 泛型 `Matrix<T>` 核心类。
- `Rational` 类型及其基本运算。
- `Matrix<Rational>`、`Matrix<int>`、`Matrix<double>` 三种矩阵类型。
- Dialog 中动态生成矩阵输入控件。
- 对三种数据类型进行输入读取。
- MainWindow 通过 action 打开输入 Dialog。
- Dialog 点击 OK 后将编辑结果返回 MainWindow。
- 使用 `QTableWidget` 显示 Current Matrix。

当前输入系统可以作为后续 Matrix Library 的“新建/编辑矩阵”入口。MainWindow 中已经迁移到 Dialog 的旧输入代码和无用头文件可以在功能稳定后清理。

## 5. 矩阵输入 Dialog

### 5.1 功能

- 选择行数、列数和数据类型。
- 根据参数 Build 输入网格。
- 输入 Rational、Int 或 Double 元素。
- Clear 当前输入。
- OK：读取并验证输入，成功后关闭并返回矩阵。
- Cancel：放弃本次修改，不影响 Current Matrix 或 Library。

### 5.2 正确的数据流程

```text
打开 Dialog
→ 选择类型和尺寸
→ Build 输入网格
→ 用户输入
→ OK
→ load_input()
→ 更新 editing_matrix_
→ accept()
→ 调用方取得矩阵
```

读取必须发生在 `accept()` 之前，否则 MainWindow 可能先取得旧矩阵。

### 5.3 后续输入校验

- Rational 分母不能为空时按默认值 1 处理，或者要求用户明确输入。
- 分母不能为 0。
- `toInt()` 和 `toDouble()` 应检查转换结果。
- 输入不合法时 Dialog 保持打开，并指出具体单元格。
- Build 会重建输入网格，应明确提示未保存输入可能丢失。
- 编辑已有矩阵时，Dialog 应预填原有数据，而不是只显示 placeholder。

## 6. Matrix Library

Matrix Library 是整个程序的持久数据中心。它保存用户主动存入的矩阵，并在下次启动程序时恢复。

### 6.1 Library 中的矩阵记录

每条记录建议包含：

| 字段 | 作用 |
|---|---|
| ID | 稳定且唯一的内部标识，不依赖显示名称 |
| Name | 用户可见名称，例如 A、Rotation2D |
| Data Type | Rational、Int 或 Double |
| Rows / Columns | 矩阵尺寸 |
| Values | 矩阵元素 |
| Created At | 创建时间 |
| Modified At | 最后修改时间 |
| Notes / Tags | 后续可选扩展 |

名称可以重复时必须通过 ID 区分；如果不允许重名，应在保存时提示用户选择 Replace 或 Rename。

### 6.2 Library 界面

建议使用左侧可停靠面板：

```text
Matrix Library
────────────────────────
A          Rational 3×3
B          Double   2×2
Basis      Rational 3×3
────────────────────────
[New] [Edit] [Duplicate] [Delete]
```

基本交互：

- 单击：选中矩阵并显示预览。
- 双击：打开编辑 Dialog。
- New：打开空白输入 Dialog。
- Edit：编辑副本，确认后更新对应记录。
- Duplicate：创建新 ID 和新名称，数据相同。
- Delete：需要确认；以后可以加入回收站。
- 右键菜单：发送到 Row Operations、作为 Left Operand、作为 Right Operand、作为 Mapping Input 等。

### 6.3 计算时的使用规则

- 工作区通过 ID 选择 Library 记录。
- 开始 Session 时复制矩阵数据。
- Session 中的修改不自动写回 Library。
- 结果保存时生成新记录，或由用户明确覆盖指定记录。

## 7. 存档系统

### 7.1 第一阶段：自动持久化 Matrix Library

第一版使用 JSON 文件保存 Library：

- 程序启动时加载。
- 新建、编辑、删除或替换记录后自动保存。
- 文件放在 Qt 提供的应用数据目录，而不是源码目录或 build 目录。
- 使用安全写入方式，避免程序中途退出导致文件损坏。
- 文件需要保存 schema version，便于未来升级格式。

Rational 应保存为结构化数据：

```json
{
  "numerator": 2,
  "denominator": 3
}
```

示意格式：

```json
{
  "version": 1,
  "matrices": [
    {
      "id": "stable-id",
      "name": "A",
      "type": "rational",
      "rows": 2,
      "columns": 2,
      "values": [
        [
          { "numerator": 1, "denominator": 2 },
          { "numerator": 0, "denominator": 1 }
        ],
        [
          { "numerator": 3, "denominator": 1 },
          { "numerator": 4, "denominator": 5 }
        ]
      ]
    }
  ]
}
```

### 7.2 第二阶段：Workspace Archive

以后可以支持完整工作区存档：

- 多个矩阵。
- 当前选中的工作区。
- Calculation Sessions。
- Step History。
- 未保存结果。
- 页面布局和 Dock 状态。

Matrix Library 负责日常自动保存；Workspace Archive 负责用户主动导入、导出、备份和分享完整项目。两者不应在第一阶段混为一套复杂系统。

## 8. Matrix Overview / Current Matrix

Matrix Overview 用于查看当前选中的 Library 矩阵：

- 显示名称、类型、尺寸。
- 使用只读表格显示元素。
- 提供 Edit、Duplicate、Delete 和 Send To 等入口。
- 不直接通过表格修改数据；编辑统一进入 Dialog。

Current Matrix 不再是应用中唯一的矩阵，而是 Matrix Library 当前选中的记录或工作区当前使用的矩阵。

## 9. Row Operations 工作区

这是专门在一个矩阵副本上进行操作的学习与演算模式。

### 9.1 工作区组成

```text
Source Matrix / Working Matrix
────────────────────────────────────────
Step History        │ Step Comparison
步骤列表            │ Before → After
────────────────────────────────────────
Manual Operations   │ Auto Operations
```

### 9.2 Step History

示例：

```text
Step 0: Original Matrix
Step 1: Swap R1 and R2
Step 2: R1 ← R1 / 2
Step 3: R2 ← R2 − 3R1
...
Final: RREF
```

默认选中最后一步，因此只想看结果的用户可以立即看到最终答案。点击任意步骤时：

- 左侧或上方显示上一步矩阵。
- 右侧或下方显示当前步骤矩阵。
- 高亮本次改变的行或单元格。
- 显示规范化的操作说明。

Step 0 没有上一步，只显示 Original Matrix。

### 9.3 手动初等行变换

第一版支持：

- Swap：`Ri ↔ Rj`。
- Scale：`Ri ← kRi`。
- Add：`Ri ← Ri + kRj`。

用户通过结构化控件选择操作、行号和系数，点击 Execute 后生成一个新 Step。

未来可以支持文字表达式输入，例如：

```text
R2 = R2 - 3R1
```

文字必须先解析成结构化操作，再执行。不能只把用户输入的字符串当成 Step。

### 9.4 拖动换行

- 只允许拖动行标题或专用拖动手柄。
- 拖动目标行需要高亮。
- Drop 的语义固定为交换两行，而不是插入并移动其他行。
- 完成后自动生成 `Swap Ri and Rj` Step。
- 单个单元格不能被自由拖动，避免破坏矩阵结构。

### 9.5 自动计算与手动计算统一

用户可以：

- 完全手动执行步骤。
- 从原矩阵直接 Auto RREF。
- 手动执行几步后，选择 Auto Complete。
- 查看程序自动生成的所有步骤。

自动算法和手动操作必须生成相同类型的 Matrix Operation 和 Step，不能维护两套独立算法结果。

### 9.6 历史行为

- 点击旧步骤默认只查看，不改变 Working Matrix。
- Undo 撤销最后一个实际步骤。
- Redo 可以在后续版本加入。
- 如果用户希望从旧步骤重新开始，应提供明确的 `Continue From This Step`。
- 第一版可以在继续旧步骤时删除其后的分支；以后再考虑树状分支历史。

## 10. Matrix Operations 工作区

用于两个矩阵之间的运算：

```text
[Matrix A ▼]  [ + / − / × ▼ ]  [Matrix B ▼]  =  Result
```

第一阶段支持：

- 加法。
- 减法。
- 矩阵乘法。

规则：

- 运算前检查尺寸兼容性。
- A 和 B 不被修改。
- Result 初始是临时结果。
- 用户选择 Save as New、Replace Existing 或 Discard。
- 结果可以发送到 Row Operations 或 Linear Mapping。

Matrix Operations 默认直接展示结果。可以提供可折叠的 Calculation Details：

- 加减法按对应元素解释。
- 乘法可以按结果单元格展示点积过程。
- 不要求与 Row Operations 使用完全相同的 Before/After 页面。

## 11. Linear Mapping 工作区

用于表达“变换矩阵把输入映射到输出”：

```text
Transformation A       Input X          Output
┌──────────────┐      ┌────────┐      ┌────────┐
│      A       │  ×   │   X    │  →   │   AX   │
└──────────────┘      └────────┘      └────────┘
```

功能方向：

- 从 Library 选择 Transformation Matrix。
- 选择向量或包含多个向量的矩阵作为 Input。
- 显示 Output。
- 检查维度是否匹配。
- 保存输出为新矩阵。
- 以后可加入二维/三维几何可视化，但不属于第一版范围。

## 12. 统一的 Calculation Session

不同工作区的 UI 不同，但可以共享 Session 概念：

| Session | 输入 | 输出 | 是否天然具有 Step History |
|---|---|---|---|
| Row Operation Session | 一个矩阵 | Working / Final Matrix | 是 |
| Binary Operation Session | 两个矩阵 | Result Matrix | 可选 |
| Mapping Session | 变换矩阵 + 输入 | Output Matrix | 可选 |

Session 保存本次计算上下文，但不代表 Library 中的永久记录。只有用户主动保存结果，才进入 Library。

## 13. Matrix Operation 与 Step 的概念

### 13.1 Matrix Operation

Operation 是真正可以执行的结构化命令，例如：

- SwapRows(rowA, rowB)
- ScaleRow(row, factor)
- AddScaledRow(targetRow, sourceRow, factor)

Operation 负责表达“做什么”。界面文字只是由 Operation 生成的说明。

### 13.2 Step

一个 Step 至少需要表达：

- Step 编号。
- Operation 类型和参数。
- 用户可读说明。
- 操作前矩阵或对前一状态的引用。
- 操作后矩阵。
- 可选高亮信息，例如受影响的行、pivot 位置。

第一版矩阵规模较小时，可以直接保存每一步的矩阵快照，换取简单可靠的 Previous、Next、Undo 和任意步骤查看。以后矩阵很大时，再考虑只保存 Operation 并重放历史。

## 14. 数据类型规则

应用支持：

- Rational：精确分数，适合行变换、RREF、inverse 和 determinant。
- Double：适合近似数值计算，需要考虑浮点误差和显示精度。
- Int：适合整数输入和封闭于整数的运算，但除法可能产生非整数。

需要明确的产品规则：

- `Matrix<int>` 执行 `Ri ← (1/2)Ri` 时不能继续以 int 保存。
- Row Operations 中遇到分数结果时，建议自动提升为 Rational，并明确提示用户。
- 或者第一版限制 Int 的某些行变换，但这会削弱学习体验。
- Double 的“等于零”和 pivot 判断以后需要使用容差，而不是严格比较。

当前建议：行变换模式优先使用 Rational；Int 在需要除法时自动提升为 Rational。该规则仍需最终确认。

## 15. 软件职责划分

建议按职责逐步拆分，而不是把全部逻辑继续堆入 MainWindow：

```text
Core
├── Matrix<T>
├── Rational
└── 数学运算

Model
├── MatrixVariant / MatrixValue
├── MatrixRecord
├── MatrixOperation
├── CalculationStep
└── CalculationSession

Services
├── MatrixLibrary
├── MatrixSerializer
└── Calculation / Trace 生成

UI
├── MainWindow
├── MatrixInputDialog
├── MatrixLibraryPanel
├── MatrixTableView
├── RowOperationsPage
├── MatrixOperationsPage
└── LinearMappingPage
```

MainWindow 负责页面协调，不负责实现所有数学算法、文件格式和输入解析。

## 16. 错误处理与用户反馈

需要避免直接依赖 `assert` 处理用户可触发的错误，因为 Release 构建可能关闭断言，而且用户需要可理解的提示。

常见错误：

- 分母为 0。
- 输入不是合法数字。
- 行列尺寸不兼容。
- 对非方阵求 determinant 或 inverse。
- 矩阵不可逆。
- Double 计算结果接近奇异。
- 从 Library 删除正在被 Session 使用的矩阵。
- 存档文件损坏或版本不兼容。

错误应显示在 Dialog、工作区提示区域或 Status Bar 中，并尽量指出具体位置和解决方式。

## 17. 第一版范围

### 第一版必须具备

- 新建和编辑 Rational、Int、Double 矩阵。
- Matrix Library 中的命名、选择、复制和删除。
- Library 自动保存和启动加载。
- Current Matrix 只读预览。
- Row Operations 的三种基本行变换。
- Step History 和 Before / After 查看。
- 自动 RREF 并生成步骤。
- 双矩阵加、减、乘。
- 结果 Save as New / Replace / Discard。

### 第一版可以暂缓

- 文字形式的行操作解析。
- 树状步骤分支。
- 完整 Workspace Archive。
- 云同步。
- 二维/三维映射动画。
- 超大矩阵优化。
- 插件系统。

## 18. 分阶段实现路线

### 阶段 0：输入系统收尾

- 确认 Dialog 的 OK 顺序为 load → accept → MainWindow 接收。
- 确认 Dialog 关闭后正确释放。
- 清理 MainWindow 中已经迁移的旧输入代码。
- 为输入失败设计明确返回方式。

验收：三种类型均可从 Dialog 输入，并在 MainWindow 正确显示。

### 阶段 1：MatrixRecord 与内存 Library

- 设计 MatrixRecord。
- 将单个 `matrix_` 扩展为多个命名矩阵。
- 在内存中支持 Add、Update、Duplicate、Remove、Find。

验收：不重启程序时可以管理多个矩阵，并在 Current Matrix 区切换。

### 阶段 2：JSON 持久化

- 确定 version 1 文件格式。
- 实现三种类型的序列化和反序列化。
- 启动加载，修改后安全保存。
- 对损坏文件提供错误提示和备份策略。

验收：关闭并重新打开程序后，保存的矩阵仍然存在且数据一致。

### 阶段 3：Matrix Library UI

- 左侧 Library 面板。
- New、Edit、Duplicate、Delete。
- 矩阵预览和选择。
- 新建/编辑 Dialog 与 Library 串联。

验收：用户不接触文件系统即可管理存档矩阵。

### 阶段 4：手动 Row Operations

- Calculation Session 和 Step 数据结构。
- Swap、Scale、Add 操作。
- Step History 列表。
- Before / After 显示。
- Undo Last Step。

验收：用户可以完全手动把一个小矩阵化到 RREF，并查看每一步。

### 阶段 5：自动 RREF Trace

- 自动算法生成与手动相同的 Operation。
- 支持 Auto RREF 和 Auto Complete From Current。
- 最终结果默认选中。

验收：自动和手动产生的步骤可以在同一个历史界面查看。

### 阶段 6：拖动换行

- 行标题拖动。
- 目标行高亮。
- Drop 生成 SwapRows Operation。
- 支持撤销。

验收：拖动两行后矩阵正确交换，并自动增加一步历史。

### 阶段 7：Matrix Operations

- 从 Library 选择 A 和 B。
- 加、减、乘及尺寸校验。
- Result 管理。
- 可选 Calculation Details。

验收：原矩阵不被修改，结果可以保存为新矩阵。

### 阶段 8：Linear Mapping

- Transformation、Input、Output 三栏。
- Library 选择和维度检查。
- 输出保存。

验收：用户可以选择变换矩阵和输入矩阵，并得到可保存的映射结果。

### 阶段 9：Workspace Archive 与体验完善

- 保存完整 Session 和页面状态。
- 导入、导出和备份。
- 快捷键、主题、布局恢复。
- 更完整的 Undo / Redo。

## 19. 需要继续讨论的决策

- [ ] Library 是否允许矩阵重名？
- [ ] 新建矩阵时名称是否必填？
- [ ] Edit 是直接更新原记录，还是默认生成副本？
- [ ] Int 在出现分数时是否自动提升为 Rational？
- [ ] Double 默认显示多少位小数？
- [ ] Calculation Session 是否需要自动保存？
- [ ] 点击旧 Step 后，`Continue From This Step` 是删除后续步骤还是建立分支？
- [ ] Matrix Library 使用 Dock、固定侧栏还是独立页面？
- [ ] Row Operations 的 Before / After 使用左右布局还是上下布局？
- [ ] 自动 RREF 是否允许用户选择 pivot 策略？
- [ ] Mapping 第一版是否只做数值输出，还是同时做二维几何可视化？

## 20. 产品成功标准

第一阶段的成功不是支持最多运算，而是形成一条稳定、容易理解的数据流：

```text
创建矩阵
→ 保存到 Library
→ 选择矩阵进入合适的工作区
→ 执行直接或逐步计算
→ 查看结果和过程
→ 决定保存、替换或丢弃
→ 下次启动仍可继续使用
```

只要这条主流程稳定，后续增加 determinant、inverse、solve、null space、column space 和几何可视化时，都可以自然接入现有架构。
